# タイピング工場 — プロジェクト規約

## 仕様の在り処（最重要）

**現行のゲーム仕様は `docs/spec/` が唯一の真実。**

| 質問 | 見る場所 |
|---|---|
| 「いまどう動いているか」 | **`docs/spec/`** → 無ければ**コードを読む**。憶測で答えない |
| 「なぜそうなったか」 | `docs/plans/<id>/proposal.md` と git 履歴 |
| 「これから何を作るか」 | `docs/roadmap.md` |

### 守ること

1. **仕様を聞かれたら `docs/spec/` を最初に読む。** `docs/spec/glossary.md` に用語定義がある
2. **ロジックを変えたら、同じコミットで `docs/spec/` を更新する。** 実装だけ直して資料を放置しない
3. **`docs/spec/` に書いていないことは、コードで確認してから答える**
4. **版ごとの企画書（旧 `docs/vNN/`）は全廃した。** 復活させない。過去版の内容が必要なら git 履歴を見る
5. **効いていない機能を「効く」と書かない。** 定義だけあって未接続のものは `docs/spec/` に「未接続」と明記する

### なぜこのルールがあるか

版ごとの企画書（旧 `docs/vNN/spec.md`）を現行仕様として読んだことで、事故が繰り返された：

- 「ローンチ広告 売上 ×1.5」と書かれていたが、実装は初動のみ ×1.5（総売上では +10%）＝**表示の詐称**
- 軸「売上予測」が売上に効くと書かれていたが、到達不能なデッドコードだった
- 「品質の肝は完成後のポリッシュ」と書かれていたが、その画面は stub で存在しない
- 「開発 +0.60 LoC/秒」と画面に表示していたが、自動開発機能は誰も呼んでいなかった

**資料が実装より古いと、プレイヤーに嘘をつく実装が生まれる。**

---

## 開発の進め方

コードを1行でも変える前に **`dev-flow` スキル**を参照する（計画→実装→検証→レビュー依頼の4ゲート）。
GO をもらう前に実装を始めない。

---

## 詳細な規約

| 対象 | 参照先 |
|---|---|
| ドキュメント運用 | `docs/CLAUDE.md` |
| コード構造（ロジックと副作用の分離） | `logic-architecture` スキル / `docs/architecture.md` |
| テスト | `testing-rules` スキル / `docs/testing.md` |
| ゲームの目的・設計思想 | `game-design` スキル |
| UI 実装規格 | `game-ui-design` スキル |
| オフィス画面の世界観 | `office-visual-design` スキル |
