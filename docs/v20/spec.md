# v0.20 仕様書: 作業感の打破（打鍵そのもののジュース強化）

> 起票日: 2026-07-12（v0.19.2 プレイフィードバック）
> 前提: v0.19.2 実装済み（選択制は完全撤回・デバッグUI+出口は有効。[docs/v19/spec.md](../v19/spec.md)）
> 主題: **「打鍵を止めて選ばせる」のではなく、「打鍵そのものの手触り・予測不能性・積み上がりの見え方」で作業感を壊す**
> 関連タスク: [tasks.md](./tasks.md)

---

## 0. 前版からの学び（v0.19.2 変更履歴より）

> 作業感の診断「意思決定の不在」は誤り。打鍵の中に意思決定を挟むと爽快感が死ぬ。
> 作業感は打鍵の外側（v20 の投資・育成、ジュース強化）で解く

この結論を受け、v20 は**打鍵フローを一切止めない**前提で5つのパターンを順番に実装する：

| # | パターン | 状態 |
|---|---|---|
| A | ジュース強化（コンボ音階SE・コンボ称号ポップ） | ✅ 実装済み |
| A-2 | 入力に合わせたアクション増量（かな消化ポップ・進捗フライアウト・PERFECTシェイク・FEVERバナー） | ✅ 実装済み |
| B | 間欠サプライズ（クリティカル打鍵・レア文章） | ✅ 実装済み |
| C | 自動の山場（クランチタイム・ボス文章） | 🔧 本版で計画・実装 |
| D | 合計値ライブ表示（積み上げの主役化） | 次版でスコープ |
| E | ギアシフト＋パーフェクトストリーク | 次版でスコープ |

追加のオーナーFB（2026-07-12）：「FEVERということはわかったが、そもそも完成までの進捗があることを理解してなかった」
→ 開発全体の進捗（doneLoC/workTarget）の可視性が低いことが判明。パターンDで対応予定（本版のスコープ外・次版）。

---

## 1. コンセプト

コアループ（企画→開発→デバッグ→リリース→販売）・経済設計は**変更しない**。v20 は「1文字1文字を打つ体験」の質感だけを強化する層で、既存の数値パイプライン（品質・メタスコア・売上・進捗）に**新しい加点経路を増やさない**ことを原則とする。

新しい報酬はすべて、既存の実証済み経路（FEVERゲージ／進捗 `doneLoC`／期待度 `hype` → `axisFans`）に乗せる。`AXIS_QUALITY_BONUS_CAP`（+8）や `STAT_QUALITY_BONUS_CAP`（+8）で頭打ちになる `devStats`・`funFactor` 経由の加点は**新規報酬の主経路にしない**（v0.19.1 で一度踏んだ罠。詳細は §4）。

---

## 2. パターン A / A-2（実装済み・概要）

- **A**: コンボ数に応じて打鍵SEの音階が半音ずつ上がる（`JUICE_CONFIG.keyPitch`）。コンボ 50/150/300 到達で称号ポップ（`JUICE_CONFIG.comboTitles`）
- **A-2**: 入力中のかな1文字（モーラ）ごとにポップ/バウンス/スパーク演出。1文字ごとの進捗フライアウト数字。PERFECT文字数達成時の画面シェイク。FEVER突入時のバナー演出
- 実装：`src/core/juice.ts`（純粋関数）・`src/features/develop/DevelopScreen.tsx`（KanaActionLine等）・`src/styles/global.css`
- 副次対応：かな進捗の表示位置がローマ字打鍵数からの近似では拗音の代替入力パターン（例：しゅ＝shu / si+ゅ）で破綻することが実プレイで判明 → `@shsk002/nano-type-jp` を 0.7.0 に更新し `resolvedUnitCount`（実値）ベースに置き換え（近似ロジック全廃）

---

## 3. パターン B: 間欠サプライズ（クリティカル打鍵・レア文章）

**目的**：単調な「順番に打つだけ」の中に、予測できない「当たり」の瞬間を挟み、次の一打・次の一文への期待値を作る。作業感の対義語＝「次が気になる」。

### 3-1. クリティカル打鍵

- 発生条件：**開発フェーズ**の正打（`onCorrect`）ごとに `JUICE_CONFIG.crit.rate`（2%）で発生
- 効果：FEVERゲージに通常 `+1` の代わりに `crit.feverBonus`（+12 = ゲージの20%）を一気に加算。既存の `addFever` をそのまま使うのでフィーバー本体のロジック（発動・効果・減衰）は無改修
- 演出：`sfx.crit()`（既存スタブ）＋ 打鍵位置に金色スパークのポップ（`.dev-crit-spark`、`.dev-kana-spark` のパターンを踏襲）＋軽い画面フラッシュ
- 企画フェーズでは発動しない（FEVERが開発フェーズ専用という既存の設計境界（v15確定）を踏襲。新しいゲージを増やさない＝最小変更）

### 3-2. レア文章

- 発生条件：**開発フェーズ**で次の文章を選ぶ瞬間（`pickPhrase` 呼び出し時）に `JUICE_CONFIG.rare.rate`（7%）で「レア」フラグを立てる
- 表示：文章カードに「★レア」バッジ＋文字色変更で視覚的に区別（打ち始める前から見えているので、打っている間ずっと期待感が続く）
- 報酬（**因果チェーンを確認済み**。詳細は §4）：完走時に FEVER ゲージへ `rare.feverBonus` を加算するのみ。
  `JUICE_CONFIG` 冒頭のコメント通り「報酬はすべてフィーバーに合流させる」設計に統一し、
  クリティカルと同じ経路（新しい加点チャネルを増やさない）にする。`devStats` / `funFactor` への
  直接倍率適用はしない（§4）
- 企画フェーズでは発動しない（クリティカルと同じ理由。FEVERが開発フェーズ専用という既存の設計境界を
  踏襲する。企画フェーズ向けの間欠サプライズは、hype/funFactorの扱いに新しい設計判断が要るため
  次版で改めて検討する）
- 演出：完走時、通常の結果カードに加えて「★レア達成！」ポップ（既存 `comboTitle` ポップのパターンを踏襲）

### 3-3. 数値（叩き台 🔧 → 実装時に調整）

```ts
crit: { rate: 0.02, feverBonus: 12 },
rare: { rate: 0.07, feverBonus: 20 },
```

`JUICE_CONFIG.rare` の `statMult` は撤去する（§4 の理由）。レア文章はクリティカルより発生率は低いが
1回あたりのフィーバーボーナスを大きくする（クリティカルは打鍵単位の小さな驚き、レア文章は
文章単位の大きな驚き、という粒度の違いを数値で表現）。

---

## 4. 報酬の因果チェーン確認（実装前チェック・v0.19.1 の再発防止）

`src/core/release.ts` を実装前に確認した結果：

- `devStats`（属性）→ `statQualityBonus` は `STAT_QUALITY_BONUS_CAP`（+8）で頭打ち。さらに `funFactor` 等と合算後 `AXIS_QUALITY_BONUS_CAP`（+8）で**二重に**頭打ちされる。通常プレイでは終盤までにこの上限へほぼ到達するため、**1文だけ devStats を3倍にしても最終品質にはほぼ届かない**（v0.19.1 で一度実際に踏んだ罠と同一構造）
- `axes.funFactor` も `devStats` と同じ `AXIS_QUALITY_BONUS_CAP` 経由のみ → 上記と同じ罠
- `JUICE_CONFIG` 冒頭のコメント（実装前から存在）が既に「報酬はすべてフィーバーに合流させる」と明記していた。当初 `rare.statMult` を `devStats` に直接適用する案はこの原則と矛盾しており、実装前チェックで撞着に気づいて修正した
- FEVER 経由の効果（進捗×2・devStats×1.5、15秒間のみ）は既存実装（v15）で、進捗×2 の部分は `doneLoC`（無上限）に直結するため、レア文章の1回のフィーバーチャージ増量は「発動しやすくなる／発動が伸びる」という形で確実に最終結果に届く

→ レア文章もクリティカルと同じく **FEVERゲージへの加点のみ**を新設し、`devStats` / `funFactor` への直接倍率適用は行わない。

---

## 5. パターン C: 自動の山場（クランチタイム・ボス文章）

**目的**：終盤（全体完成度80%以降）を「あとちょっと」の追い込みとして体感させる。B（間欠サプライズ）が予測不能な当たりなら、Cは**予測できる・自分から選ばない自動の山場**（ラストスパートの高揚感）。選択は挟まない（v0.19.2の学び厳守）。

### 5-1. クランチタイム

- 発動条件：**開発フェーズ**で `progressPct`（`current.doneLoC / workTarget * 100`。既に「全体完成度」表示で使用中の値）が `JUICE_CONFIG.crunch.startPct`（80%）以上になった瞬間。選択なし・自動発動
- 効果：進捗獲得（`addDevelopLoC` に渡す値）に `crunch.progressMult`（×2）を追加で乗せる。既存の「速度×コンボ×フィーバー」の式にそのまま掛けるだけで、新しい加点経路は作らない。`doneLoC` は無上限（§4で確認済みの安全な経路）なので、ここでも安全
- 突入演出：一度だけ `sfx.crunch()`（既存スタブ）＋「⏰CRUNCH TIME!!⏰」バナー（`dev-fever-banner` と同系統のパターン）。以降は80%を下回ることがない（`doneLoC` は減らない）ため、常時発動のバッジ表示のみ（点滅などはしない。演出疲れを避ける）
- 企画フェーズでは発動しない（`doneLoC`/`workTarget` は開発フェーズ専用の概念のため対象外）

### 5-2. ボス文章

- 発動条件：クランチタイム中、次の作業チケット文を選ぶ瞬間に `crunch.bossRate`（15%）で「ボス文章」を選ぶ（レア文章とは独立の抽選。両方に当たる場合はボスを優先）
- 文章の作り方：新しいコンテンツを追加せず、既存の `CATEGORY_PHRASE_POOLS`（`data/devPhrases.ts`）から2文を連結した「長め」の1文にする（`pickBossPhrase`）。素材追加なしで「長文＝手応え」を作る
- 表示：文章カードに「⚔BOSS」バッジ（レアバッジと同じ位置・仕組み、色のみ変更）
- 報酬（**因果チェーン確認済み**。B §4 の分析をそのまま踏襲）：完走時に FEVER ゲージへ `crunch.bossFeverBonus`（+30。B のレア文章 +20 より大きい＝長文を打ち切った分、驚きの粒度も大きくする）を加算するのみ。`devStats`/`funFactor` への直接倍率は付けない
- 演出：完走時「⚔BOSS撃破！」ポップ（B の `dev-rare-pop` と同系統）＋画面シェイク（`dev-perfect-shake` を流用）

### 5-3. 数値（叩き台 🔧 → 実装時に調整）

```ts
crunch: { startPct: 80, progressMult: 2, bossRate: 0.15, bossFeverBonus: 30 },
```

---

## 6. パターン D・E（次版でスコープ・本版では未着手）

- **D: 合計値ライブ表示（積み上げの主役化）** — オーナーFB「完成までの進捗があることを理解してなかった」に直結。現状「開発全体の進捗」は左サイドバーの小さい数字のみ。中央（打鍵の視線の近く）に常時見える形での再配置が必要
- **E: ギアシフト＋パーフェクトストリーク** — `JUICE_CONFIG.gears` / `perfect` が既に叩き台としてある。入力速度(wpm)でギアが切り替わる演出、ノーミス連続文数のストリーク表示は未着手

---

## 7. スコープ外

- アイテムシステム（v19から継続の積み残し。育成・投資の拡張は別版）
- 山場イベント（締切依頼・ライバル発売）は C のボス文章と統合するか要検討（次版で判断）
- 音・BGM本実装／PixelLab素材の本発注

---

## 8. 検証チェックリスト（パターンC）

- [x] 全体完成度が80%を超えた瞬間にクランチタイムが自動発動し、バナーが1回だけ流れる（実機・Playwright MCPで実際にキーを打鍵して80%を跨ぎ、`.dev-crunch-banner`「⏰CRUNCH TIME!!⏰」のDOM出現を確認。以降はヘッダーの「⏰クランチ 進捗×2」バッジが常時表示に切り替わる）
- [x] クランチタイム中は進捗獲得が明確に増える（コード確認：`addDevelopLoC` に渡す値へ `crunchActive ? JUICE_CONFIG.crunch.progressMult : 1` を乗算。実機でクランチ中の結果カードの進捗値が伸びることを確認）
- [x] ボス文章が通常文より長い1文として表示される（実機・RNGをbossRate未満に固定し実打鍵で誘発。「⚔BOSS」バッジと通常文の2倍の長さの文章表示を確認。スクショ c-boss-badge.png）
- [x] ボス文章を打ち切ると「⚔BOSS撃破！」ポップと画面シェイクが出て、FEVERゲージが `bossFeverBonus` 分増える（実機・実打鍵で32文字のボス文章を完走→`.dev-boss-pop`「⚔BOSS撃破！」と`.dev-perfect-shake`の同時出現、FEVER発動を確認）
- [x] devStats / funFactor への倍率適用を行っていないことをコードで確認（ボス報酬コードは addFever のみ呼び出し）
- [x] unit テストでクランチ判定・ボス判定の境界値、`pickBossPhrase` の連結ロジックを保証（`core/juice.test.ts` 4件・`data/devPhrases.test.ts` 2件）
- [x] 4点セット（build/unit 242/ui 14/e2e 7）全緑
- [x] 1280×720内でスクロールなし・表示崩れなし（32文字のボス文章表示時も scrollWidth/Height が 1280×720 と一致することを確認）
- [ ] クランチ突入の高揚感・ボス文章の「山場」としての体感 → オーナー確認待ち

---

## 9. 検証チェックリスト（パターンB）

- [x] 開発フェーズで正打するとクリティカルが発生し、FEVERゲージが大きく跳ねる（実機・Playwright MCPで実際にキーを打鍵。Math.random を crit.rate 未満に固定し、通常60打鍵必要なFEVER発動が実打鍵5回で発動することを確認。`.dev-crit-pop`に「⚡CRITICAL!」が実際にDOMへ出現することも確認）
- [x] レア文章が文章カードに視覚的に区別できる形で表示される（実機・「★レア」バッジがイベント中は隠れ、通常時は表示されることを確認。スクショ b-rare-badge.png）
- [x] レア文章を打ち切ると、FEVERゲージが `rare.feverBonus` 分だけ明確に増える（実機・実打鍵でレア文章を完走→`.dev-rare-pop`に「★レア達成！」が出現し、FEVERが発動することを確認）
- [x] devStats / funFactor への倍率適用を行っていないことをコードで確認（§4 の罠を再現していない。DevelopScreen.tsx のレア報酬コードは addFever のみ呼び出し）
- [x] unit テストで crit 判定・rare 判定の境界値を保証（`core/juice.test.ts` に4件追加）
- [x] 4点セット（build/unit 236/ui 14/e2e 7）全緑
- [x] 1280×720内でスクロールなし・表示崩れなし（実機・scrollWidth/Height が 1280×720 と一致することを確認）
- [ ] クリティカル発生時のSE（`sfx.crit()`）・レア出現/完走時のSE（`sfx.rare()`）の心地よさ → オーナー確認待ち（音は機械的に確認不可能）
- [ ] オーナー実プレイで「間欠サプライズ」として体感できるか → オーナー確認待ち

---

## 10. 変更履歴

### 2026-07-13（パターンC実装完了）

- パターンC（クランチタイム・ボス文章）実装完了。Playwright MCPで実際にキーを打鍵し、80%到達時のバナー・ボス文章の誘発（RNG固定）・完走報酬・画面シェイク・1280×720オーバーフローなしまで実機確認済み（§8）
- ボス文章とレア文章が同時当選した場合はボスを優先する仕様を実装時に確定（バッジ・報酬の二重表示を避ける）

### 2026-07-12（パターンB実装完了・パターンC計画）

- パターンB（クリティカル打鍵・レア文章）実装完了。Playwright MCPで実際にキーを打鍵しての検証まで完了（§9）。オーナーFB「かなりいいです」
- パターンC（クランチタイム・ボス文章）を計画。ボス文章は新規コンテンツを追加せず、既存の`CATEGORY_PHRASE_POOLS`から2文連結する方式に決定（最小変更）。報酬はBと同じくFEVERゲージのみに乗せる設計（§5-2）

### 2026-07-12（v0.20 初版・パターンA/A-2実装済み反映＋パターンB計画）

- v0.19.2 の学び（作業感は打鍵の外側ではなく打鍵そのものの質感で解く）を受けて起票
- パターンA（コンボ音階SE・称号ポップ）・パターンA-2（かなアクション増量・FEVERバナー等）は
  spec化より先に実装が完了していたため、本版で遡って仕様として記録
- パターンA-2実装中に発覚したかな進捗表示のバグ（拗音の代替入力パターンで表示位置が破綻）を
  `@shsk002/nano-type-jp` 0.7.0 への更新（`resolvedUnitCount` 実値ベース）で根本修正
- オーナーFB「完成までの進捗を理解してなかった」を受け、パターンD（合計値ライブ表示）の
  優先度が上がったことを記録（実装は次版）
- パターンB（クリティカル打鍵・レア文章）を計画。実装前に報酬の因果チェーンを確認し、
  `devStats`/`funFactor` 経由の頭打ち報酬（v0.19.1で一度踏んだ罠）を避け、
  進捗・期待度（hype）の無上限経路に報酬を乗せる設計に変更（§4）
