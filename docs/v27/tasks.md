# v0.27 タスク — タイピングお題の拡充（厳密テーマ×10倍）

凡例：`[x]` 実装済 / `[~]` 部分実装 / `[ ]` 未着手。spec: [spec.md](./spec.md)。

> 方針確定（オーナー決定 2026-07-20）：**厳密テーマを維持したまま打鍵プールを10倍**。供給ロジックは無改修（データ追加のみ）。文面は Claude が生成。

## 0. 方針（確定済み・spec §5）

- [x] 5-1 連続重複回避 → (a)データのみで確定（10倍で自然解消）
- [x] 5-2 難易度帯 → 短/中/長を均等配分
- [x] 5-3 目標件数 → 開発 各80・企画 各40・メモ 各5
- [x] 5-4 ジャンル別打鍵文 → 別版送り

## 1. 打鍵プールの増量（データ）

- [x] `CATEGORY_PHRASE_POOLS` 各カテゴリを 8 → 80 件に拡充（program / graphics / sound / design 各72件を新規生成）
  - [x] 各カテゴリで短(6〜9)/中(10〜13)/長(14〜18)を概ね均等に配分
  - [x] 厳密テーマ（作業を正確に説明）を維持
  - [x] 抽象カテゴリ（design）は作業対象を広く展開して言い換え被りを散らす
- [x] `PLAN_PHRASE_POOLS` 各カテゴリを 4 → 40 件に拡充（concept〜sales の7種、各36件を新規生成）
- [x] `PLAN_MEMO_POOLS` 各カテゴリを 3 → 5 件に拡充（補助のため控えめ）

## 2. 供給ロジック

- [x] 無改修で確定（(a)採用）。`pickPhrase`/`pickPlanPhrase`/`pickBossPhrase` と `DevelopScreen.tsx` は触らない

## 3. テスト（testing-rules unit=node 層）

- [x] `src/data/phrasePools.test.ts` 新規：件数下限アサート（開発≥80 / 企画≥40 / メモ≥5）
- [x] ひらがな限定検査（`^[ぁ-んゔー]+$`）を全打鍵プールに適用（メモは表示専用のため除外）
- [x] プール内 完全重複なし の検査
- [x] 既存 `devPhrases.test.ts` が緑のまま（`pickBossPhrase`）

## 4. 検証（dev-flow [3]／レビュー依頼前に全部）

- [x] 4点セット：`npm run build` / `test:unit`(324) / `test:ui`(14) / `test:e2e`(7) 全緑
- [x] playwright-verify：企画で新規追加文「いちばんのみせばをきめる」を実キー入力で完走→次文へ遷移。開発フェーズで program プール文「あたりはんていをつくる」描画を確認。console エラー 0
- [x] 1280×720・スクロールなし（scrollW/H=1280/720）・表示崩れなしを目視
- [x] spec §7 の完成条件を消し込み

## 継承タスク（前版から）

- なし（v0.27 新規テーマ）
