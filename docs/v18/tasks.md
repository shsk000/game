# v0.18 実装タスク

> 仕様: [spec.md](./spec.md)　凡例: `[x]` 実装済 / `[~]` 部分実装 / `[ ]` 未着手
> 規約: logic-architecture / testing-rules スキル準拠

## 継承タスク（前版から）

- [x] 旧セーブの累計売上引き継ぎの決着 → セーブ v7 移行で圧縮＋解放再計算（下記 4）
- [ ] デバッグ負荷の体感確認（オーナー実プレイ）→ 必要なら §4-2 のレバーで調整

## 追加タスク（このバージョン）

### 1. 経済カーブ再設計（spec §1）
- [x] unlockSalesRequired が**未参照の死に設定だったのを発見し有効化**（unlockNextScale に累計売上ゲート追加）
- [x] 閾値確定（シミュレーションで調整）: mobile ¥3000万 / indie ¥2億 / hit ¥20億 / aaa ¥250億。unlockCost も圧縮
- [x] `core/progressionSimulation.test.ts`：平均モデル60作×3seed。「mobile 8〜16作目」「aaa 25〜60作目」「ティア間隔≥2作」を恒久ガード

### 2. 目標ウィジェット（spec §2）
- [x] `core/goals.ts`：nextGoals => 3件（次の規模解放/次Lv社員/発見組合せ）＋unit テスト6件
- [x] オフィスに 🎯「つぎの目標」PixelWindow 常設（お知らせ直下・3行・達成は✓強調）
      ※ラベルは「発見した組合せ」（「図鑑」表記はメニューとセレクタ衝突するため）

### 3. リリース結果のアドバイス（spec §3）
- [x] `core/advice.ts`：3要素の最小値で1件指摘（同点はキャラ→相性→タイピング優先）＋テスト5件
      ※クラウドエージェント（v18-advice）の成果物を回収して完成
- [x] 売上画面（STEP2）末尾に 📈1行表示

### 4. 旧セーブ移行（spec §4-1・オーナー承認済み）
- [x] セーブ v7：lifetimeRevenue ÷100＋規模/ジャンル/テーマ解放を圧縮後累計で再計算。
      v1〜v6 すべての旧パスが v7 圧縮に合流。テスト（1.5億全解放 → 150万・mini再ロック）

### 5. 検証
- [x] vitest 228件 / e2e 7本 / build 緑 / 実機（🎯ウィジェット3行表示・残額/exp/組合せ）
- [ ] オーナーへプレイ依頼
