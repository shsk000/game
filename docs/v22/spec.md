# v0.22 仕様書: 採用ガチャ — 引く楽しみ

> 起票日: 2026-07-19（プロデューサー計画）
> **2026-07-19 オーナー承認**: 本計画でいったん確定（価格は規模スケール制に改訂済み）。
> 同日オーナーGOで実装着手。**v0.21（投資機能）は未マージのまま先行実装し、後取り込みで
> 統合する**（オーナー指示）。base の main には v21 ブランチ前半のジャンル27種・テーマ28種
> 拡充のみ取り込み済み。ガチャ価格の最終調整（§10-2）は v21 統合後に再検証する
> 親計画: [docs/roadmap.md](../roadmap.md) §v0.22（採用ガチャ・オーナー指定）
> 関連タスク: [tasks.md](./tasks.md)

---

## 0. 前提・課題認識（現状実装の確認済み事実）

本版の設計は以下のコード上の事実に乗せる（調査日 2026-07-19。行番号は当時の main。
実装時は v21 マージ後のコードで再確認する）。

| 項目 | 現状 |
|---|---|
| 候補生成 | `newCandidate()`（`src/data/employees.ts`）。basePower は `CANDIDATE_POWER_RANGE`＝**0.2〜0.6 の一様乱数**。ランクの概念なし |
| specialty | primary 1つ（ボーナス3〜10）＋40%で2つ目（1〜4） |
| 採用コスト | 候補の初月給 `wage` を即時徴収＋以後毎月給与。`wage = ¥30万 + power×¥60万 + (level−1)×¥5万` |
| 候補リフレッシュ | `REFRESH_COST`＝**¥50**（実質無料。v21 spec §5 で「統合判断は v22 で」と先送り済み） |
| 採用UI | OfficeScreen の採用モーダル。静的テキストのみ、演出なし |
| charPower | `computeCharacterScore()`：powerSum/規模別CAP×70（上限70）＋役割多様性（最大10）。**score 上限100・規模別 POWER_CAP が天井** |
| 分布ガード | `src/core/balanceSimulation.test.ts`：序盤70+＝0%／中盤80+＝0%／95+は終盤のみ（≤15%） |
| 進行ガード | `src/core/progressionSimulation.test.ts`：mobile解放8〜16作目・aaa到達25〜60作目・60作でLv7+ |
| 採用上限 | 12人（ワークステーション数と連動） |

**課題（roadmap 原文）**：採用は「候補を見る→雇う」の作業で、引きの快感がない。候補の能力差
（0.2〜0.6）はあるのに、ランク・希少性・演出として体験化されていない。

---

## 1. 事業目的と前提

最上位の目的（game-design スキル）：広告収益・広告は人質にしない・長く遊ぶ人が一番得する・課金なし。

本版が解く問い（roadmap 原文）：**「採用を『作業』から『引きの快感』にできるか？」**

- ガチャは**ゲーム内資金のみ**で引く。リアルマネー・広告視聴の強制は一切絡めない
  （広告連動の無料ガチャは v0.24 の広告本実装まで保留。§5）。
- メタ進行3本柱のうち「会社が育つ（経営）」の採用体験を、評価開示（メタスコアガチャ）と
  同型の「開ける瞬間の快感」に揃える。

---

## 2. コンセプト

**この版で変えたこと**：採用フローを「候補1名提示＋¥50リフレッシュ」から「**資金を払って引く
社員ガチャ**」に置き換える。能力の抽選レンジ・成長・給与・charPower の各パイプラインは
**一切変更しない**。変えるのは「候補がどう生成され、どう提示されるか」だけ。

v21 で確立した設計原則を継承する：
- **新しい数値経路（ボーナス種別）を増やさない**。ランクは basePower 帯と specialty 構成の
  **ラベル**であり、ランク自体が品質・売上に加点する経路は作らない。
  → roadmap の「S＋固有ボーナス?」は**採用しない**（CAP罠の回避。差は basePower/specialty のみ）。
- **打鍵フローに介入しない**。ガチャは OfficeScreen（採用モーダル）内で完結する。

---

## 3. コアループへの影響

企画→開発→デバッグ→リリース→販売のループは無変更。採用の入口だけが変わる：

```
旧: 採用モーダル → 候補1名を見る →（¥50で引き直し）→ 初月給を払って雇用
新: 採用モーダル → ガチャ料を払って引く → カード開封演出（ランク開示）
     → 雇う（初月給を払う）or 見送る（候補は消える）
```

- **引き＝候補カードの排出**であり、自動雇用しない。雇用判断（初月給＋毎月給与の負担）は
  従来どおりプレイヤーに残す（B連発で枠が埋まる事故を防ぐ）。
- 見送った候補は保持しない（在庫UIを作らない。もう一度引くには再度ガチャ料）。

---

## 4. 経済設計

> **2026-07-19 オーナー指示でガチャを 2 種に分割（v0.22.1）**：
> - **ノーマルガチャ**：安価（規模連動・現行テーブル）。**S を出さない**（B/A のみ）。序盤の主力。
> - **プレミアムガチャ**：高額（mini ¥600 万＝初期資金 ¥500 万を上回り**序盤は 1 発も引けない**）。
>   S の唯一の入手源。天井 10 連。中盤以降に手が届く。
> これにより「S を序盤から引けない」＝ v16 の早期分布ガードが経済面から自然に守られる
> （premium が高すぎて序盤は S を揃えられない）。

### 4-A. ランクと排出率 🔧（種類別）

| ランク | ノーマル 🔧 | プレミアム 🔧 | basePower 帯 🔧 | specialty 🔧 |
|---|---|---|---|---|
| B | 70% | 40% | 0.20〜0.40 | primary 1つ（ボーナス3〜7）。2つ目なし |
| A | 30% | 45% | 0.40〜0.55 | primary 1つ（5〜10）＋40%で2つ目（1〜4）＝現行相当 |
| S | **0%（出ない）** | 15% | 0.55〜0.70 | **2つ確定**（primary 6〜10＋2つ目 3〜6） |

- 帯内は一様乱数（現行 `rollPower()` の丸め・仕組みを流用し、レンジだけランクで切り替える）。
- 役職（programmer/designer/pr）の抽選 `ROLE_DICE` は現行のまま（ランクと独立）。
- **期待値の変化**：現行一様 0.2〜0.6 の平均 0.40 → 新方式の平均 ≒ 0.36。平均をやや下げ
  分散を上げる（＝引きの上振れが体験として立つ）。進行への影響は §8 のシミュレーションで検証。
- **S の上限 0.7 は現行上限 0.6 より高い**。ただし charPower は規模別 POWER_CAP と
  powerBonus 上限70で天井が固定されており、S は「天井に早く到達する」だけで天井を上げない
  （roadmap の判断ゲート「S でも Lv1 なら charPower 上限は変わらない」を構造で満たす）。

### 4-B. 価格 🔧（種類別・規模スケール制）

- **単発価格は種類 × 解放済み最高規模に連動**させる（賃料 `monthlyRent` と同じ参照方式）。
  `REFRESH_COST`（¥50）は**廃止し、ガチャ料に統合**（v21 からの宿題を消化）。

  | 最高解放規模 | ノーマル 🔧 | プレミアム 🔧（≒ノーマル×） |
  |---|---|---|
  | mini | ¥5万 | ¥600万（×120・初期資金超で序盤引けない） |
  | mobile | ¥50万 | ¥3,000万（×60） |
  | indie | ¥800万 | ¥3億（×37） |
  | hit | ¥1.5億 | ¥30億（×20） |
  | aaa | ¥15億 | ¥150億（×10） |

- **プレミアムの序盤ロック**：mini ¥600万 は初期資金 ¥500万 を上回るので開始直後は 1 発も
  引けない。倍率は序盤ほど大きく（×120→×10）、序盤は「まず引けない」・終盤は「気軽に回せる
  吸収先」になる。これで S が序盤に出ない＝早期分布ガードを経済で守る（§8）。
- ノーマル価格アンカーは**既存 `SCALE_BALANCE.devCost` の約1/6**（固定額だと中盤以降に
  実質無料化するため規模スケール制）。給与式（power×¥60万）は規模スケールしないため、
  終盤の実コストはガチャ料が支配的＝「引く価格」がそのまま規模の重みになる。
- **雇用コストは従来どおり**（初月給即払い＋毎月給与）。S は wage 式により自然に高給
  （Lv1 で ¥72万/月）＝「金に飽かせて S を並べる」ことへの既存ブレーキが効く。
- 10連は**本版では実装しない**（§5）。

### 4-C. 天井（ピティ）🔧（叩き台・軽量案）

- **20連続で S 非排出なら次の1回を S 確定** 🔧。カウンタは S 排出でリセット。
- 目的：確率の下振れで「引く楽しみ」が「恨み」に転じるのを防ぐ（長く遊ぶ人が一番得する）。
- カウンタはセーブに永続化（§7 セーブ影響）。実装コストが小さい単純カウンタ方式のみ。
  演出上のカウント表示（「あと N 回で S 確定」）は任意 🔧。

---

## 5. スコープ外（本版では作らない）

- **10連ガチャ**：候補在庫UIが必要になり複雑化。単発の手応えを見てから判断（次版以降）。
- **広告連動（無料ガチャ1日1回等）**：広告SDKは v0.24 で本実装。モック広告で入れると
  「広告は人質にしない」の検証ができないため、v0.24 の広告設計に委ねる。
- **S 固有ボーナス（新規数値経路）**：§2 のとおり採用しない。
- **ガチャ限定キャラ・期間限定排出**：コンテンツ量産が必要。エンドゲームは v0.23 の主題。
- **PixelLab 新規素材の発注**：まず既存トーン（CSS＋既存ドット絵）で開封演出を成立させる。
  素材発注が必要と判断した場合はオーナー承認を取ってから（発注リストを提示）。

---

## 6. UX

- **場所**：OfficeScreen の採用モーダルを改修（新画面は作らない）。
- **開封演出**（カード開封・ランク開示）：
  1. 「ガチャを引く ¥{規模別価格}」ボタン → カード裏面が出現
  2. ランク色のフレーム発光（B=銅 / A=銀 / S=虹 🔧 配色は実装時に game-ui-design 規約で確定）
  3. カードめくり → 名前・役職・能力・specialty・ランクバッジ表示
  4. S のみ追加フラッシュ＋紙吹雪（既存の特大ヒット演出部品を流用）
- SE：既存SE資産の流用のみ（新規音源は v0.24 の主題）。
- 開封後に「雇用する ¥{wage}」「見送る」の2択。満席（12人）時は雇用ボタン無効化（現行踏襲）。
- 演出は**スキップ可能**（連続で引くときにテンポを殺さない。クリックで即結果）。
- 1280×720 固定内・スクロールなし（既存UI規約）。

---

## 7. 技術メモ

- 純粋ロジックは `src/core/gacha.ts`（仮）に置く：`rollRank(rng, pityCount)` /
  `newCandidateWithRank(rng, rank)`。`newCandidate()` は内部でレンジを受け取る形にリファクタし
  重複を作らない。rng は既存のシード注入方式を踏襲（テスト可能に）。
- 排出率・帯域・規模別価格テーブル・天井は `src/data/balance.ts` に `GACHA_CONFIG` として
  まとめ、叩き台/確定をコメントで区別（INVEST_CONFIG と同型）。価格の参照規模は
  `currentRent` と同じ「解放済み最高規模」判定を流用する。
- **セーブ影響**：ピティカウンタ（int 1個）の追加。既存の候補スロット（`Candidate`）は
  「開封済み・未雇用カード」の保持に流用可能か実装時に判断 🔧。セーブバージョンは
  現行スキーマに単純フィールド追加で済むならマイグレーション最小（デフォルト0で補完）。
- `REFRESH_COST` と関連UI（「別の候補 ¥50」）は削除。参照箇所を grep で全消し。
- テストは testing-rules 規約に従い、境界値（排出率の合計=100%・帯域境界・ピティ発火・
  満席時・資金不足時）を優先。

---

## 8. 判断ゲート（roadmap 原文＋本書で具体化)

> **2026-07-19 実装時の重要な発見とオーナー決定（この節は当初計画から更新）**
>
> 実装中のシミュレーションで、当初計画の前提「S でも charPower 天井は変わらないから序盤ガードは
> 破れない」が**部分的に誤り**だと判明した。ピティ（20連S確定・mini価格¥5万なら最大¥100万/体、
> 初期資金¥500万で3体を強制入手可能）を使うと、新規プレイヤーが初日に **S 3体**を揃えられ、
> mini の低い POWER_CAP（2.5）を飽和させて charPower 56〜69 → **mini でメタ70+ が出る**。
> 実測では S 下限 0.55×3 でもメタ70+が約48%出るため、「S 上限を 0.65 に下げる」程度では
> 解決しない（"保証された高power社員3体"という構造そのものの問題）。
>
> **オーナー決定＝「許容」**：これは v21「時間を金で買う（先取り・加速）」思想に沿った
> **意図された課金ショートカット**として認める。ただし最終天井は維持される（下記1で担保）。
>
> **v0.22.1 追記（ガチャ2種分割で大幅緩和）**：S の入手源をプレミアム（mini ¥600万＝初期資金
> 超）に限定したため、**序盤に S を揃えること自体が経済的にほぼ不可能**になった（開始直後は
> premium を 1 発も引けず、mini 中に ¥600万 を貯める頃には mobile 目前）。ノーマルは A 上限
> 0.55 までなので、上でも触れた「A×3 でも早期メタ70+ は出うる」構造は残るが、S ほどの飽和は
> 起きない。結果として早期分布ガードは当初計画より自然に守られる。

1. **分布ガード維持（核の不変条件）**：`balanceSimulation.test.ts` の序盤 persona は
   **非課金の平均プレイヤー（power 0.4×3）**をモデル化し「序盤 メタ70+＝0%」を維持する。
   - S 課金ラッシュ（3体）による序盤メタ70+はガード対象外＝許容（上記決定）。
   - **最終天井の担保**：aaa では S-Lv1 3体でも powerSum 2.1／POWER_CAP 3.6 ＝ charPower ≈51 で
     メタ95に届かない。「95 の壁はレベル成長でしか越えられない」不変条件は課金でも破れない。
     壊れるのは序盤ティアの通過速度だけで、これは加速思想の範囲。
2. **進行ガード維持**：`progressionSimulation.test.ts`（mobile 8〜16作・aaa 25〜60作・
   60作Lv7+）も**非課金平均プレイヤー**（初期 power 0.4）をモデル化し緑を維持する。
   ガチャ課金ラッシュの早期加速はこのガードに反映しない（オーナー決定）。
3. **「引きの快感」の成立**はオーナー実プレイでのみ確認可能（機械検証不可・§9末尾）。

---

## 9. 検証チェックリスト（完成条件・実装時に1つずつ消し込む）

- [x] ガチャを引くと資金がガチャ料分だけ減り、カード開封演出の後に候補が表示される
      （実機：mini ¥5万を引き funds ¥5,000,000→¥4,950,000、裏面→めくり→候補表示を確認）
- [x] 排出率がB/A/S＝70/25/5%で、basePower・specialtyがランク仕様帯に収まる
      （`gacha.test.ts`／`employees.test.ts` で3万試行の実測率と帯域境界を検証）
- [x] 20連S非排出→次回S確定（ピティ）が発火し、S排出でカウンタがリセットされる
      （unit＋store test＋実機：pity=20で強制S、S後 pity=0 を確認）
- [x] 「雇用する」で初月給が引かれ社員が増える／「見送る」で候補が消えガチャ料は返らない
      （実機：雇用で−¥480,000・社員+1・候補消去／見送りで候補消去・資金不変を確認）
- [x] 資金がガチャ料未満のとき引けない／満席（12人）時は雇用ボタンが無効化される
      （実機：funds 0 で「ガチャを引く」ボタン disabled。満席は isFull バインドで既存踏襲）
- [x] REFRESH_COST と旧リフレッシュUIが完全に消えている（grep で src 全体ゼロを確認）
- [x] ピティカウンタがセーブ・ロードをまたいで保持される（旧セーブはデフォルト0で読める）
      （実機：pity=1・候補Bを保存→リロード→復元を確認。storage.test で旧v7補完もテスト）
- [x] 演出スキップ（クリックで即結果）が機能しテンポを殺さない
      （実機：裏面 button クリックで即 front 化を確認）
- [x] 分布ガード・進行シミュレーションのテストが更新後の生成方式で緑（§8）
- [x] 4点セット（build/unit/ui/e2e）が全て緑（unit 266／ui 14／e2e 7 全緑）
- [x] Playwright MCP で実際にガチャ→開封→雇用/見送りを操作し実測確認
- [x] 1280×720内でスクロールなし・表示崩れなし（開封演出中も含む）を目視確認
      （scrollWidth==clientWidth・overflow なしを実測）
- [ ] 「引きの快感」が成立しているか → **オーナー確認待ち（機械検証不可）**

---

## 10. 未決事項まとめ 🔧

1. 排出率・basePower帯・specialty構成の最終値（§4-A は叩き台。シミュレーションで調整）
2. 規模別価格テーブル（¥5万〜¥500万）の最終値（v21 込みの経済で再検証）
3. 天井の閾値（20連）と「あとN回でS確定」表示の要否
4. ランク別フレーム配色・演出の詳細（game-ui-design 規約で実装時に確定）
5. 候補スロットを「開封済みカード」保持に流用するかどうか（セーブ設計の詳細）
6. ~~S帯上限 0.7 が分布ガードを破った場合の調整方針~~ → **決着（2026-07-19）**：破れることが
   判明したが、S 課金ラッシュによる序盤加速は「許容」（§8 冒頭）。S 帯は 0.55〜0.7 のまま維持。
