# v0.22 実装タスク: 採用ガチャ

> 凡例：`[x]` 実装済 / `[~]` 部分実装 / `[ ]` 未着手
> 仕様: [spec.md](./spec.md)
> **状態: 計画承認済み・2026-07-19 オーナーGOで実装着手**

---

## 継承タスク（前版から）

- [ ] REFRESH_COST（候補リフレッシュ¥50）の扱い確定 — v21 spec §5 から先送りされた宿題。
      本版で「廃止・ガチャ料に統合」として消化する（spec §4-B）
- [ ] 備品（equipment.ts 死コード）の活用判断 — v21 A/B実装後のプレイFB待ち。本版でも保留

## 追加タスク（このバージョン）

### 1. データ層・純粋ロジック

- [ ] `balance.ts` に `GACHA_CONFIG` 追加（排出率70/25/5・basePower帯・specialty帯・
      規模別価格テーブル¥5万〜¥500万・ピティ閾値20。全て🔧コメント付き叩き台）
- [ ] `core/gacha.ts` 新設：`rollRank(rng, pityCount)`（ピティ発火込み）
- [ ] `employees.ts` リファクタ：`rollPower` / `rollSpecialties` をランク別レンジ受け取りに
      一般化し、`newCandidateWithRank(rng, rank)` を追加（重複ロジックを作らない）
- [ ] `REFRESH_COST` 定義と全参照の削除

### 2. store 配線

- [ ] `gameStore` に `pullGacha` アクション（資金チェック→ガチャ料減算→ピティ更新→候補生成）
- [ ] 「見送る」アクション（候補破棄。返金なし）／既存 `hireCandidate` はそのまま流用
- [ ] ピティカウンタの state 追加とセーブ・ロード（旧セーブはデフォルト0で補完）

### 3. UI（OfficeScreen 採用モーダル改修）

- [ ] 旧「別の候補 ¥50」UI撤去、「ガチャを引く ¥{規模別価格}」ボタン設置（資金不足時は無効化）
- [ ] カード開封演出（裏面→ランク色フレーム→めくり→結果表示。B=銅/A=銀/S=虹 🔧）
- [ ] S排出時の追加演出（既存の特大ヒット演出部品・SEを流用。新規素材なし）
- [ ] 演出スキップ（クリックで即結果）
- [ ] 開封後の「雇用する ¥{wage}」「見送る」2択（満席時は雇用無効化・現行踏襲）
- [ ] 1280×720 固定内・スクロールなしを維持

### 4. テスト（testing-rules 規約）

- [ ] unit: 排出率の合計=100%・大量試行での実測率が設定±誤差内
- [ ] unit: ランク別 basePower / specialty が仕様帯に収まる（帯域境界値）
- [ ] unit: ピティ（20連で次回S確定・S排出でリセット）
- [ ] unit: 資金不足で引けない／見送りで返金されない
- [ ] **分布ガード更新**: `balanceSimulation.test.ts` の序盤 persona を S上限（0.7×3）に
      更新し「序盤70+＝0%」維持を実測（spec §8-1。破れたら帯を再調整）
- [ ] **進行ガード更新**: `progressionSimulation.test.ts` にランク生成＋ガチャ料支出を反映し
      mobile 8〜16作・aaa 25〜60作・60作Lv7+ の緑を維持（spec §8-2）
- [ ] セーブ移行: 旧セーブ読み込みでピティ0初期化されることのテスト

### 5. 検証（verify フロー）

- [ ] 4点セット（build/unit/ui/e2e）全緑
- [ ] Playwright MCP 実機操作（ガチャ→開封→雇用/見送り→資金反映）
- [ ] spec §9 チェックリストの消し込み
- [ ] オーナー実プレイ確認（「引きの快感」成立・価格感）
