# v0.24 実装タスク

> 仕様: [spec.md](./spec.md)　凡例: `[x]` 実装済 / `[~]` 部分実装 / `[ ]` 未着手
> 規約: logic-architecture / testing-rules / dev-flow スキル準拠
> 前提: v0.23 は不採用。本版が最終計画版（ローンチ準備）。
> スコープ調整（2026-07-19）: セーブのエクスポート/インポートは不採用。収益化（広告SDK・デプロイ）は着手保留。
> **本版で実装するのは A.音・B.磨きの2系統。**

## 継承タスク（前版から）

- [ ] オーナー実プレイで「作業感」が減ったかを確認（v0.19〜継続）
- [ ] v21 積み残し（社員上位研修／備品投資／REFRESH_COST統合）は本版スコープ外・別途判断

## 追加タスク（このバージョン）

### A. 音（spec §3-A）

- [ ] `gameStore` にミュート/音量状態を追加＋永続化（`Persisted` 反映・version bump 要否判断）
- [ ] 設定モーダル（`OfficeScreen.tsx:914`）にミュートトグル＋音量スライダーを追加
- [ ] `sound.ts` のミュート方式を `sfx.ts` に統合し、死にコード `sound.ts` を削除
- [ ] SE を ReleaseScreen（リリースファンファーレ）・レベルアップ・実績解放へ展開
- [ ] BGM ループ再生機構を新規実装＋画面別切替（音源方針は spec §9-1 で確定後）
- [ ] unit/実機：ミュート反映・永続化・各SEの発火を確認

### B. 磨き: チュートリアル・タイトル（spec §3-B）

- [ ] `Tutorial.tsx` を刷新し、初回起動で確実に出る導線へ（PlanScreen 依存を解消）
- [ ] `Screen` union に `'title'` 追加＋初期値/boot 分岐（`App.tsx`）でセーブ有無により出し分け
- [ ] タイトル画面 UI を新設（1280×720・スクロールなし）
- [ ] 実機：新規/継続それぞれの初回導線を確認

### 検証（spec §7-8）

- [ ] 4点セット（build/unit/ui/e2e）全緑
- [ ] Playwright MCP 実機検証（音量・初回導線）
- [ ] 音の心地よさ・BGM・手触り → オーナー確認待ち（機械検証不可）

## C. 収益化: 広告SDK・デプロイ — 着手保留（オーナー指示 2026-07-19。後日フェーズ）

> 本版では着手しない。以下は着手時のタスク雛形として残す。

- [ ] （着手前）デプロイ先＋広告SDK選定（自前静的＋H5 Ads / ポータル配信＋各SDK）← オーナー決定（spec §9-5）
- [ ] （着手前）広告配置リストの確定（roadmap「4箇所」× spec「6リワード＋強制1」× 実装「5箇所」）
- [ ] 実SDK provider を実装し `setAdProvider` で差し替え（視聴完了コールバック後付与＋フォールバック）
- [ ] リワード5箇所の実SDK動作確認／未配線3つの配線判断
- [ ] レビューゲート：広告OFFで全規模クリア可能を検証
- [ ] デプロイ構成（`vite base`／ホスティング／CI／`index.html` meta）＋本番実機確認

## 積み残し・スコープ外（次版検討）

- セーブのエクスポート/インポート（本版で不採用・次版判断）
- PWA/オフラインアプリ化
- マルチプレイ/ランキングサーバー（サーバーレス維持のため恒久スコープ外）
- 課金
