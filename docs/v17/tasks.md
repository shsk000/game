# v0.17 実装タスク

> 仕様: [spec.md](./spec.md)　凡例: `[x]` 実装済 / `[~]` 部分実装 / `[ ]` 未着手
> 規約: logic-architecture（ロジックは core/ に純粋関数＋rng/clock 注入）／testing-rules（ロジック変更は unit テスト必須・スクロール禁止）

## 継承タスク（前版から）

- [x] v0.16 バランス再設計（実装済み。オーナープレイ確認で本タスクの FB を受領）

## 追加タスク（このバージョン）

### 1. リリース結果の 2 画面分割（spec §1）
- [ ] ReleaseScreen を STEP1「評価」/ STEP2「売上」に分割（既存演出・広告機能は移設）
- [ ] 両ステップが 1280×720 に収まる（overflow なし）ことを確認
- [ ] ユースケーステスト更新（STEP1 メタスコア → STEP2 売上 → オフィス）

### 2. 全員参加（spec §2）
- [ ] startProject を全員参加に（assignedEmployeeIds は内部で全員を積む）
- [ ] PlanScreen のアサイン UI 削除 →「参加メンバー: 全員（N人）」表示
- [ ] computeCharacterScore の超過ペナルティ廃止（未満 -10 は維持）＋テスト更新
- [ ] 分布シミュレーションを全員参加前提に更新し帯が崩れないことを確認

### 3. 能力・レベルの可視化（spec §3）
- [ ] 役割効果の説明を ROLE_EFFECT から生成する共通ヘルパー（表示3箇所：採用/社員リスト/企画）
- [ ] PlanScreen にチーム効果プレビュー（開発速度/品質/売上/バグ抑制）
- [ ] Lvアップバッジに効果差分（power・給与）を併記
- [ ] 社員リストに次レベルまでの exp ゲージ
- [ ] 月給式にレベル項を追加：+ (level−1)×¥5万 🔧（balance.ts / growth.ts / テスト更新）

### 3.5 タイトル命名（spec §3.5）
- [ ] PlanScreen にタイトル入力欄＋🎲 再抽選（generateTitle の組み合わせ生成）
- [ ] startProject に title 引数（空はランダム補完）＋テスト

### 4. バグ発生システム（spec §4）
- [ ] `core/bugs.ts` 新設：発生判定（ミス/コード起因×抑制率）・デバッグ工数・残バグペナルティ（純粋関数＋rng注入＋テスト）
- [ ] CurrentProject に bugCount。開発中の蓄積（useTyping の fail / フレーズ完走にフック）
- [ ] HUD の 🐛×N カウンタ＋発生ポップ
- [ ] テストフェーズ：テストチケット 3 枚 → バグ発覚演出
- [ ] デバッグフェーズ：bugCount×2 文の修正チケット。全滅で noBugs ボーナス／`[このまま発売]` で残バグペナルティ（品質−2/匹・炎上+2/匹）
- [ ] 旧バグ抽選（triggerBugIfDue・devPhrases の12%バグ文）を廃止し一本化
- [ ] release への合流（残バグ → quality 減点・reputationRisk 加算）＋テスト

### 5. 検証（spec §6）
- [ ] 全画面スクロールなし / アサイン操作なしで開始 / 可視化一式 / バグの因果が体感できる
- [ ] 分布シミュレーション緑のまま / unit＋use-case 追加分緑
- [ ] 検証4点セット＋オーナーへプレイ依頼
